import 'package:flutter/material.dart';

class AppHeaders extends StatelessWidget {
  const AppHeaders({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
