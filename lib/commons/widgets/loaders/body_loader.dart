import 'package:flutter/material.dart';

class BodyLoader extends StatelessWidget {
  const BodyLoader({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
